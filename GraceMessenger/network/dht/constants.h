#pragma once

namespace GraceMessenger
{
	namespace DHT
	{
		const size_t ALPHA = 3;
		const size_t K = 512;
		const size_t BUF_SIZE = 1024 * 4;
		const size_t TTL = 5;
		const size_t PING_PERIOD = 60000;
		const size_t EXPIRED_TIME = 300000;
		const size_t BAD_PERIOD = 130000;
		const size_t FIND_PERIOD = 20000;

	}
}